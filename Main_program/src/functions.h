#pragma once

#include "roboruka.h"
#include "RBControl_pinout.hpp"

/*


//Define motor pins
#define LEFT M8
#define RIGHT M6

void encRead(int& left, int& right);
void encReset(int& left, int& right);
void encPrintValue(int& left, int& right);
void motorLeft(int power);
void motorRight(int power);
void motorStopAll();
void motorPP();
int inRange(int power, int max = 100);
void driveLeft(int position, int power);
void driveRight(int position, int power);
void corssRegulator(int power, float P = 0.5);
bool driveToRegulator(int encLeft, int encRight, int driveTo, int power, float P = 0.1);
bool driveRegulator(int drive, int maxPower, float P = 0.1);


void ultraPrintValue();

*/